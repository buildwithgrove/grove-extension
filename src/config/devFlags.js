/**
 * Developer Flags
 * Toggled via Makefile targets (e.g., `make dev_force_inject_on`)
 * Only takes effect when dev mode is enabled (testnet/localhost)
 */
var GROVE_DEV_FLAGS = {
  forceInject: true
};
